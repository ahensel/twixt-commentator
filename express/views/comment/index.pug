if comment
  #preview
    if preview
      .previewHeader
        include _comment_header
    else
      .commentHeader
        include _comment_header
    != prepareComment(comment.comment)
    br
    br
  script.
    scrollToBottom();
