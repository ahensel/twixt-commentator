<span class="comment-header-text">
  On
  <% if (comment.created_on) { %>
    <%
      const createdDate = new Date(comment.created_on).toISOString();
    %>
    <%= createdDate.slice(0, 10) %> at <%= createdDate.slice(11, 16) %>,
  <% } %>
  <% if (author) { %>
    <b><%- h2(author.name) %></b>
    <a href="/user/info/<%= author.id %>">
      <img class="info" src="/images/info.gif" alt="(info)" width="9" height="9">
    </a>
  <% } %>
  <% if (preview) { %>
    considered saying:
  <% } else { %>
    said:
  <% } %>
</span>
<% if (!preview && comment.id && session && session.user_id && author && session.user_id === author.id) { %>
  <span class="comment-edit-delete-links">
    <% if (comment.deleted_at) { %>
      <a href="" onclick="undeleteComment(<%= comment.id %>); return false;">Undelete</a>
    <% } else { %>
      <a href="" onclick="editComment(<%= comment.id %>); return false;">Edit</a>
      <a href="" onclick="deleteComment(<%= comment.id %>); return false;" title="Soft Delete (can be undone)">Delete</a>
    <% } %>
  </span>
<% } %>
